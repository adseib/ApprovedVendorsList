/*
Deployment script for ApprovedVendorsList

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "ApprovedVendorsList"
:setvar DefaultFilePrefix "ApprovedVendorsList"
:setvar DefaultDataPath "C:\Users\andre\AppData\Local\Microsoft\VisualStudio\SSDT\ApprovedVendorsList"
:setvar DefaultLogPath "C:\Users\andre\AppData\Local\Microsoft\VisualStudio\SSDT\ApprovedVendorsList"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
MERGE INTO companyaddress AS Target 
USING (VALUES 
        (1, 'Street1', 'City1'), 
        (2, 'Street2', 'City1'), 
        (3, 'Street3', 'City2')
) 
AS Source (AddressID, Street, City) 
ON Target.AddressID = Source.AddressID 
WHEN NOT MATCHED BY TARGET THEN 
INSERT (Street, City) 
VALUES (Street, City);


MERGE INTO vendor AS Target
USING (VALUES 
        (1, 'Tibbetts', 'Donnie', '2013-09-01'), 
        (2, 'Guzman', 'Liza', '2012-01-13'), 
	(3, 'Catlett', 'Phil', '2011-09-03')
)
AS Source (VendorID, CompanyName, ContactName, AddDate)
ON Target.VendorID = Source.VendorID
WHEN NOT MATCHED BY TARGET THEN
INSERT (CompanyName, ContactName, AddDate)
VALUES (CompanyName, ContactName, AddDate);


MERGE INTO Enrollment AS Target
USING (VALUES 
	(1, 2.00, 1, 1),
	(2, 3.50, 1, 2),
	(3, 4.00, 2, 3),
	(4, 1.80, 2, 1),
	(5, 3.20, 3, 1),
	(6, 4.00, 3, 2)
)
AS Source (EnrollmentID, Grade, AddressID, VendorID)
ON Target.EnrollmentID = Source.EnrollmentID
WHEN NOT MATCHED BY TARGET THEN
INSERT (Grade, AddressID, VendorID)
VALUES (Grade, AddressID, VendorID);
GO

GO
PRINT N'Update complete.';


GO
